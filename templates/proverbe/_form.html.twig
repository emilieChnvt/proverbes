{{ form_start(form, {'attr': {'class': 'max-w-md mx-auto p-6 bg-white rounded shadow-md'}}) }}

<div class="mb-4">
    {{ form_label(form.file, null, {'label_attr': {'class': 'block text-gray-700 font-semibold mb-2'}}) }}
    {{ form_widget(form.file, {'attr': {'class': 'block w-full text-sm text-gray-600'}}) }}
    {{ form_errors(form.file) }}
</div>

<button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded">
    Enregistrer
</button>

{{ form_end(form) }}
